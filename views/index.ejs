<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>PDF Chat</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="icon" type="image/x-icon" href="/images/favicon.ico">
  <style>
    #chatBox div {
      margin-bottom: 10px;
    }

    .ai-response strong {
      color: #f39c12;
    }

    .ai-response em {
      color: #d63384;
    }

    .ai-response code {
      background: #f8f9fa;
      padding: 2px 4px;
      border-radius: 4px;
      font-family: monospace;
    }

    /* Sidebar container and animation */
    .sidebar-container {
      position: fixed;
      top: 0;
      left: 0;
      width: 250px;
      height: 100vh;
      background: #f8f9fa;
      border-right: 1px solid #dee2e6;
      padding: 4.5rem 1rem;
      overflow-y: auto;
      display: flex;
      flex-direction: column; /* Ensure the elements are stacked vertically */
      transition: transform 0.3s ease;
      z-index: 1040;
    }

    .sidebar-container ul a li{
      text-decoration: none !important;
    }

    .sidebar-container.collapsed {
      transform: translateX(-100%);
    }

    .main-content {
      margin-left: 250px;
      width: calc(100vw - 250px);
      transition: all 0.3s ease;
    }

    .main-content.full-width {
      margin-left: 0;
      width: 100vw;
    }

    .toggle-sidebar-btn {
      position: fixed;
      top: 1rem;
      left: 1rem;
      z-index: 1050;
      border-radius: 0.375rem;
      padding: 0.5rem 0.75rem;
      background-color: #0d6efd;
      color: white;
      border: none;
    }

    .toggle-sidebar-btn:hover {
      background-color: #0b5ed7;
    }

    .active{
      background-color: #3574d2;
      color: whitesmoke;
    }


    /* Style the "Your PDFs" section */
    #pdfList {
      flex-grow: 1; /* This makes the list take up available space */
      overflow-y: auto; /* Ensures scrolling if the list exceeds the container height */
    }

    /* Style for the "New Chat" button */
    .newChat {
      margin-top: auto; /* Pushes it to the bottom of the sidebar */
      padding: 10px 20px;
      background-color: #007bff; /* Blue background */
      color: white; /* White text */
      text-align: center;
      border-radius: 5px;
      cursor: pointer;
      font-weight: bold;
      transition: background-color 0.3s;
      text-decoration: none;
    }

    .newChat:hover {
      background-color: #0056b3; /* Darker blue on hover */
    }

    .newChat:active {
      background-color: #003d75; /* Even darker blue when clicked */
    }

    .renameBtn{
      width: 1.7em;
      height: 1.7em;
      cursor: pointer;
      margin: 0.2em;
    }
  </style>
</head>

<body class="bg-light">

  <script>
    function displayLoginModal() {
      // Show login modal on page load (only once)
      window.addEventListener("DOMContentLoaded", () => {
        setTimeout(() => {
          const loginModal = new bootstrap.Modal(document.getElementById('loginModal'));
          loginModal.show();
        }, 1200);

      });
    }
  </script>

<% if(user) { %>

  <!-- Rename Modal -->
<div id="renameModal" class="modal d-none position-fixed top-0 start-0 w-100 h-100 bg-dark bg-opacity-50 d-flex justify-content-center align-items-center">
  <div class="bg-white p-4 rounded shadow" style="min-width: 300px;">
    <h5 class="mb-3">Rename PDF</h5>
    <form id="renameForm">
      <input type="hidden" id="pdfIndex" name="pdfIndex">
      <input type="text" id="newTitle" name="newTitle" class="form-control mb-3" required>
      <div class="d-flex justify-content-end gap-2">
        <button type="button" class="btn btn-secondary" id="cancelRename">Cancel</button>
        <button type="submit" class="btn btn-primary">Save</button>
      </div>
    </form>
  </div>
</div>



  <!-- Toggle Button -->
  <button class="toggle-sidebar-btn" id="sidebarToggle">☰</button>

 
  <!-- Sidebar -->
  <div class="sidebar-container" id="sidebar">
    <h5 class="mb-3">Your PDFs</h5>
    <ul id="pdfList" class="list-group">
      <% for(let i = user.session.length; i > -1; i--){ %>
        <% ses = user.session[i]; %>
        <% if(ses) { %>
          <% if(target == -1){ %>
            <li class="list-group-item p-0 border-0 d-flex justify-between items-center">
              <a 
                href="/<%= i %>" 
                class="flex-grow d-block px-3 py-2 list-group-item-action text-decoration-none">
                <%= ses.pdfData.meta_info.Title %>
              </a>
              <img 
                src="/images/edit.png" 
                alt="Rename" 
                class="renameBtn" 
                data-id="<%= i %>" 
                data-title="<%= ses.pdfData.meta_info.Title %>">

            </li>
            
          <% }else{ %>
            <li class="list-group-item p-0 border-0 d-flex justify-between items-center">
              <a 
                href="/<%= i %>" 
                class="flex-grow d-block px-3 py-2 list-group-item-action text-decoration-none <%= Number(i) == Number(target) ? 'active' : '' %>">
                <%= ses.pdfData.meta_info.Title %>
              </a>
              <img 
                src="/images/edit.png" 
                alt="Rename" 
                class="renameBtn"
                data-id="<%= i %>" 
                data-title="<%= ses.pdfData.meta_info.Title %>">
            </li>
            
          <% } %>
        <% }else{ console.log("ses is ", ses);} %>
          
      <% } %>
    </ul>
    <a href="/" class="newChat">
      New Chat
    </a>
  </div>
  <% } %>

  <!-- Main Content -->

  <% if (user) { %>
    <div class="main-content container-fluid mt-4" id="mainContent">
  <% }else{ %>
    <div class="main-content full-width container-fluid mt-4" id="mainContent">
  <% } %>

    <div class="container">
      <h3 class="mb-4">Chat with Your PDF</h3>

      <% if(target == -1){ %>
        <!-- Upload Form -->
        <form id="uploadForm" enctype="multipart/form-data">
          <input type="file" name="pdf" class="form-control mb-2" required />
          <button type="submit" class="btn btn-primary mb-4">Upload PDF</button>
        </form>
        <% } %>


      <!-- Chat Display -->
      <% if(user){ %>
        <% if(Number(target) > -1){ %>
      <div id="chatBox" class="border p-3 mb-3 bg-white" style="height: 300px; overflow-y: auto;">
        
          <div class="ai-response"><strong>AI:</strong> Hey <em>
              <%= user.fullName %>
            </em>! How can I help you with <em><%=user.session[target].pdfData.meta_info.Title%></em>?</div>
      
      </div>
        <% } %>
      <% }else{ %>
        <div id="chatBox" class="border p-3 mb-3 bg-white" style="height: 300px; overflow-y: auto;">
        
          <div class="ai-response"><strong>AI:</strong> Hey! How can I help you?</div>
      
      </div>
      <% } %>
           
      


     


      <% if(!user){ %>
        <div class="text-center mb-3">
          <small>
            <strong><a onclick="goLogin()">Login</a></strong> to save your conversation and continue where you left.
          </small>
        </div>
        <script>displayLoginModal()</script>
        <% } %>

          <!-- Question Form -->
          <% if(user){ %>
          <% if(Number(target) > -1){ %>
          <form id="chatForm">
            <div class="input-group">
              <input type="text" id="question" class="form-control" placeholder="Ask a question..." required />
              <button type="submit" class="btn btn-success">Send</button>
            </div>
          </form>
          <% } %>
          <% }else{ %>
            <form id="chatForm">
              <div class="input-group">
                <input type="text" id="question" class="form-control" placeholder="Ask a question..." required />
                <button type="submit" class="btn btn-success">Send</button>
              </div>
            </form>
          <% } %>
    </div>
  </div>


  <!-- <div class="container-fluid mt-4">
    <div class="row">
      <div class="col-md-3 mb-3">
        <div class="border rounded p-3 bg-light" style="height: 500px; overflow-y: auto;">
          <h5 class="mb-3">Your PDFs</h5>
          <ul id="pdfList" class="list-group">
            
            <li class="list-group-item list-group-item-action active">Assignment_1.pdf</li>
            <li class="list-group-item list-group-item-action">Research_Notes.pdf</li>
          </ul>
        </div>
      </div>

      <div class="col-md-9">
        <div class="container">
          <h3 class="mb-4">Chat with Your PDF</h3>

          <form id="uploadForm" enctype="multipart/form-data">
            <input type="file" name="pdf" class="form-control mb-2" required />
            <button type="submit" class="btn btn-primary mb-4">Upload PDF</button>
          </form>

          <div id="chatBox" class="border p-3 mb-3 bg-white" style="height: 300px; overflow-y: auto;">
            <% if(user){ %>
              <div class="ai-response"><strong>AI:</strong> Hey <em>
                  <%= user.fullName %>
                </em>! How can I help you?</div>
              <% } else { %>
                <div class="ai-response"><strong>AI:</strong> Hey! How can I help you?</div>
                <% } %>
          </div>

          <% if(!user){ %>
            <div class="text-center mb-3">
              <small>
                <strong><a onclick="goLogin()">Login</a></strong> to save your conversation and continue where you left.
              </small>
            </div>
            <script>displayLoginModal()</script>
            <% } %>


              <form id="chatForm">
                <div class="input-group">
                  <input type="text" id="question" class="form-control" placeholder="Ask a question..." required />
                  <button class="btn btn-success">Send</button>
                </div>
              </form>
        </div>
      </div>
    </div>
  </div> -->


  <!-- Login Prompt Modal -->
  <div class="modal fade" id="loginModal" tabindex="-1" aria-labelledby="loginModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="loginModalLabel">Login Required</h5>
        </div>
        <div class="modal-body">
          Please login to save your conversation and access it later.
        </div>
        <div class="modal-footer">
          <a href="/login/0" class="btn btn-primary">Go to Login</a>
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Continue without login</button>
        </div>
      </div>
    </div>
  </div>

  <!-- rename script -->
  <script>
    const modal = document.getElementById('renameModal');
    const renameBtns = document.querySelectorAll('.renameBtn');
    const pdfIndexInput = document.getElementById('pdfIndex');
    const newTitleInput = document.getElementById('newTitle');
    const renameForm = document.getElementById('renameForm');
    const cancelBtn = document.getElementById('cancelRename');
  
    renameBtns.forEach(btn => {
      btn.addEventListener('click', () => {
        const index = btn.getAttribute('data-id');
        const title = btn.getAttribute('data-title');
        pdfIndexInput.value = index;
        newTitleInput.value = title;
        modal.classList.remove('d-none');
      });
    });
  
    cancelBtn.addEventListener('click', () => {
      modal.classList.add('d-none');
    });
  
    renameForm.addEventListener('submit', async (e) => {
      e.preventDefault();
      const index = pdfIndexInput.value;
      const newTitle = newTitleInput.value;
  
      const res = await fetch(`/rename/${index}`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ newTitle })
      });
  
      if (res.ok) {
        window.location.reload();
      } else {
        alert("Rename failed");
      }
    });
  </script>
  
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    const user = <%- JSON.stringify(user) %>;
    console.log("User object from server:", user);
    const sidebar = document.getElementById("sidebar");
    const mainContent = document.getElementById("mainContent");
    const sidebarToggle = document.getElementById("sidebarToggle");

    sidebarToggle.addEventListener("click", () => {
      sidebar.classList.toggle("collapsed");
      mainContent.classList.toggle("full-width");
    });

    function goLogin() {
      location.href = "/login/0";
    }

    const chatBox = document.getElementById("chatBox");

    function formatMarkdown(text) {
      return text
        .replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>')  // Bold
        .replace(/\*(.*?)\*/g, '<em>$1</em>')              // Italic
        .replace(/`(.*?)`/g, '<code>$1</code>');           // Inline code
    }

    document.addEventListener("DOMContentLoaded", function () {
  const uploadForm = document.getElementById("uploadForm");
  if (uploadForm) {
    uploadForm.addEventListener("submit", async function (e) {
      e.preventDefault();
      const formData = new FormData(this);
      const res = await fetch("http://localhost:3000/upload", {
        method: "POST",
        body: formData,
      });
      const data = await res.json();
      alert(data.message || "Uploaded!");
      location.href = "/" + data.target;
    });
  }

  const chatForm = document.getElementById("chatForm");
  const chatBox = document.getElementById("chatBox");
  const target = <%= target %>; // Injected from EJS

  if (chatForm && chatBox) {
    chatForm.addEventListener("submit", async function (e) {
      e.preventDefault();

      const questionInput = document.getElementById("question");
      const question = questionInput.value.trim();
      questionInput.value = "";

      if (!question) return;

      chatBox.innerHTML += `<div><strong>You:</strong> ${question}</div>`;
      scrollToBottomSmooth();

      const url = target === -1 ? "/ask" : `/ask/${target}`;
      console.log("Submitting to:", url);

      try {
        const res = await fetch(url, {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ question }),
        });

        const data = await res.json();
        const formattedAnswer = formatMarkdown(data.answer || "No response");
        chatBox.innerHTML += `<div class="ai-response"><strong>AI:</strong> ${formattedAnswer}</div>`;
        scrollToBottomSmooth();
      } catch (err) {
        chatBox.innerHTML += `<div class="ai-response text-danger"><strong>Error:</strong> ${err.message}</div>`;
        console.error("Error fetching AI response:", err);
      }
    });
  }
});



    // Smooth scroll to bottom
    function scrollToBottomSmooth() {
      const chatBox = document.getElementById("chatBox");
      chatBox.scrollTo({
        top: chatBox.scrollHeight,
        behavior: "smooth"
      });
    }



  </script>
</body>

</html>